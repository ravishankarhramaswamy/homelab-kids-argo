apiVersion: v1
kind: Service
metadata:
  name: {{ include "jclic.fullname" . }}
  labels:
    {{- include "jclic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "jclic.selectorLabels" . | nindent 4 }}
