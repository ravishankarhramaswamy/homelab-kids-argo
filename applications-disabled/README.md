# Disabled applications

These apps are kept for reference but are not deployed by Argo CD because the ApplicationSet only scans `applications/*`.
Move a directory back under `applications/` to re-enable it.
