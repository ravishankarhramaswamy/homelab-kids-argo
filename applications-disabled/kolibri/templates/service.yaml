apiVersion: v1
kind: Service
metadata:
  name: {{ include "kolibri.fullname" . }}
  labels:
    {{- include "kolibri.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "kolibri.selectorLabels" . | nindent 4 }}
